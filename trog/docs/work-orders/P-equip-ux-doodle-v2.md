# [패키지 P] 장비 UX 직관화 + 낙서판 v2

사용자 실플레이 제보(2026-07-15). 작업 폴더: `C:\Users\ui2030\Documents\trpg\trog`

공통 규칙:
- 시작 전 반드시 현재 코드를 먼저 읽어라(특히 O-3가 방금 고친 장비 분류·L이 만든 낙서판). 미커밋 변경 위에 얹어라.
- 기존 스타일 준수. 대규모 리팩터링 금지. git 커밋 금지. IP 고유명사 금지.

## P-1. 아이템 획득 = 소지품 직행 (자동장착 완전 제거) (main.py)

사용자 요구: 얻은 아이템이 바로 껴지지 말 것. O-3의 "불확신 시 미장착"에서 한 발 더 —
**전면 미장착**으로.
1. parse_and_apply_items의 자동장착 분기(equip_to_slot/equip_dual_to_slots 호출부)를 제거하고
   전부 인벤토리 보관(kind 분류는 유지 — P-2 장착 UI가 씀). AUTO_EQUIP_MAX_PER_RESPONSE 상수와
   관련 테스트(test_auto_equip_cap)는 새 동작에 맞게 수정/대체.
2. 획득 토스트 문구에 "→ 소지품" 뉘앙스 반영(기존 토스트 경로 재사용).
3. 대장간 등 다른 자동장착 경로가 있으면 grep으로 찾아 동일 정책 적용(명시적 사용자 행동에 의한
   장착만 허용).

## P-2. 장비 슬롯 클릭 UX — 해제·교체 (main.py + game.js + style.css)

사용자 요구: ① 장착 중인 장비를 클릭하면 뺄 수 있게, ② 빈/장착 슬롯 클릭 시 보유 장비로 교체.
1. 서버: `unequip_slot` WS 타입 신설 — 해당 슬롯 장비를 인벤토리로 이동(빈 슬롯이면 no-op 에러 무시).
   기존 수동장착 핸들러(6805~)는 그대로(장착 시 기존 장비 인벤 회수 동작 이미 있음).
2. 클라: 내 캐릭터 패널 장비 4슬롯(refreshCharPanel ~3019)을 탭 가능하게:
   - 슬롯 탭 → 바텀시트(모바일)/작은 모달(PC): 최상단 [🚫 해제 — 소지품으로](장착 중일 때만) +
     보유 장비 목록. 목록은 guessEquipSlot(O-3 미러 분류기)로 **이 슬롯에 맞는 것 먼저**, 나머지는
     "다른 종류" 접힌 그룹으로. 아이템 탭 → 기존 장착 WS 경로로 전송(교체 시 기존 장비는 자동 인벤 회수).
   - 남의 캐릭터에는 미노출. 관전자 미노출. 죽은 상태(사망) 중 장착 변경은 서버에서 거부.
3. 시각: 빈 슬롯은 "＋ 비어 있음" 점선 스타일로 탭 유도. 터치타깃 40px(B-2).

## P-3. 낙서판 v2 — 사이드 슬라이드 + 도구 확장 (main.py + game.js + style.css)

사용자 요구: 우클릭 시 옆면 슬라이드로 열기, 지우개, "프로필(초상화) 그리기처럼 세세하게".
1. **열기 방식**: 
   - PC: 게임 화면 빈 영역(서사 패널 배경 등)에서 **우클릭** → 오른쪽에서 슬라이드 인 되는
     사이드 패널(폭 ~40vw, 최소 420px)로 낙서판 표시. **주의**: 커스텀 행동 칩의 우클릭 삭제(B-5)와
     충돌 금지 — 칩·입력창·버튼 위 우클릭은 기존 동작 유지(target 검사).
   - 모바일: 우클릭이 없으므로 기존 🎨 버튼이 같은 사이드 패널(폭 ~92vw) 슬라이드로 열림.
     기존 전체화면 오버레이는 사이드 패널로 대체(모바일 드로어 transform 관용구 재사용, 2716~).
   - 열려 있어도 서사는 계속 보임(패널 밖 영역). 밖 탭/우클릭/✕로 닫기.
2. **도구 확장** (초상화 캔버스 5786~ 관용구 참조하되 코드 공유 강제 아님):
   - **지우개**: stroke에 `erase: true` 플래그 추가 — 렌더 시 globalCompositeOperation
     'destination-out'. 서버 add_doodle_stroke 검증에 erase 불리언 허용. 굵기 2단 그대로 사용.
   - **실행취소(자기 획만)**: 각 획에 서버가 증가 id 부여. `doodle_undo` WS → 서버가 그 sender의
     마지막 획 제거 → `doodle_remove {id}` 브로드캐스트 → 클라 전체 재렌더(정규화 좌표라 저렴).
     연타 폭주는 기존 레이트리밋 관용구 재사용.
   - **색 12색 + 굵기 3단**으로 확장(서버 화이트리스트 동기화).
   - 채우기(bucket)·스포이드는 스코프 밖 — 공유 리플레이 모델과 안 맞음(주석으로 사유).
3. doodle_state에 id 포함하도록 확장(L의 스키마에 id·erase 필드 추가 — 하위호환 걱정 없음, 휘발성).

## 검증 (전부)
1. py_compile / node --check.
2. 테스트 갱신·신설: _test_doodle.py(erase 플래그·undo 자기획만·id 증가), 장비(자동장착 전면 미장착·
   unequip 인벤 회수·사망 중 거부). 회귀: _test_balance, _test_equipment, _test_kill_xp, _test_act_progress.
   trpg conda 파이썬, PYTHONIOENCODING=utf-8.
3. 실동(GAME_PORT=8099) + playwright: ① 아이템 획득 → 미장착·소지품 확인 ② 슬롯 탭 → 해제/교체
   동작 ③ PC 우클릭 → 사이드 슬라이드, 칩 우클릭 삭제는 여전히 동작 ④ 지우개로 지운 것이 상대
   클라에도 지워짐 ⑤ undo가 자기 획만 제거. 모바일 360×800 + 데스크탑 스크린샷(_shots/).
4. 서버·브라우저 종료, 테스트 saves 삭제, 잔류 프로세스 체크.
5. 구현 전 mcp__codex-bridge__ask_codex 계획 검토, 구현 후 검증.
6. 같은 문제 2회 실패 시 중단하고 상황 보고.
