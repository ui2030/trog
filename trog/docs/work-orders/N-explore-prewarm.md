# [패키지 N] 탐색 배경 선(先)생성 — "배경 그리는 중"을 없앤다

윤택화 로드맵(2026-07-15 Fable 설계). 작업 폴더: `C:\Users\ui2030\Documents\trpg\trog`
(M은 모바일 UX 패스가 코드 주석에서 선점 — 지시서 알파벳은 L 다음 N.)

공통 규칙:
- 시작 전 반드시 현재 코드를 먼저 읽어라 — 특히 현재 배경 URL 생성·프리로드 경로
  (game.js `_expStageUrls`/`_expPreloadGen`/`.exp-bg-loading` 주변)를 grep으로 전부 파악.
- 미커밋 변경 위에 얹어라. 기존 스타일 준수. git 커밋 금지. IP 고유명사 금지.

설계 의도: 이미지 생성 서비스(Pollinations)는 같은 URL(프롬프트+시드)을 한 번 만들면
CDN에 캐시한다 → "미리 생성" = **URL을 미리 한 번 요청해두는 것**. 3겹 방어:

## N-1. 프리워밍 (클라 중심)

1. **탐색 시작 신호 즉시 전량 병렬**: explore 시작 수신 시 스테이지 URL 전부를 즉시 `new Image()`로
   동시 fire(현재 순차/지연 로드라면 병렬로 전환). 표시 로직(_expStageShown)은 그대로.
2. **평시 워밍 1장**: DM 응답(dm_response) 수신 시, 마지막 탐색 지형 또는 기본 지형의
   다음 스테이지 후보 URL 1장을 저우선 프리로드(Image, decoding='async'). 응답당 1장 초과 금지 —
   과요청은 외부 서비스 예의 위반. 상수 레버로 on/off(`EXP_PREWARM = true`).
3. 시드/URL 결정이 서버에 있으면(먼저 grep) 서버가 explore 시작 브로드캐스트에 URL 목록을
   동봉하는 기존 구조를 따르고, 클라 결정이면 클라에서 처리 — **현 구조를 바꾸지 말고 그 안에서 워밍만 추가**.

## N-2. 같은 장소 재사용 + 계속 생성 (방 수준 캐시)

1. place(장소명) 키로 생성했던 URL 목록 캐시 — URL 결정 주체 쪽에 둔다
   (서버면 room 딕셔너리 상한 30장소·직렬화 제외, 클라면 모듈 Map 상한 30 — 방 나가면 자연 소멸).
2. 같은 장소 재탐색: 캐시 URL 재사용(즉시 표시) + **새 스테이지 1~2장은 새 시드로 추가**
   (사용자 요구: "생성도 계속" — 같은 장소도 조금씩 새 그림이 섞이게).

## N-3. 지형별 폴백 풀

1. 지형 상수(_expTerrain 계열: dirt 등 — grep으로 실제 지형 목록 확인)별 **고정 시드 URL 2장**을
   상수로 정의. 오버레이 열리는 즉시 폴백 1장을 배경에 깔고, 본 이미지가 로드되면 크로스페이드
   (기존 .exp-bg 전환 관용구 재사용).
2. 본 이미지 로드 실패/8초 타임아웃 시 폴백 유지 + 재시도 1회. "🎨 배경 그리는 중…" 칩은
   폴백이 깔려 있으면 띄우지 마라(체감 제거가 목표).

## 하지 마라
- 이미지 바이너리를 서버 디스크에 저장하는 프록시/캐시 서버 구축 금지 — CDN 캐시로 충분(ponytail).
- 프리워밍 대량 발사 금지(응답당 1장, 시작 시 스테이지 수만큼만).

## 검증 (전부)
1. node --check static/game.js (+ 서버 수정 시 py_compile + 회귀 _test_exploration 계열).
2. playwright로 탐색 진입 2회(같은 장소): 1회차 각 스테이지 이미지 onload까지 ms 로그,
   2회차(캐시) 동일 측정 — before/after 수치 보고. 폴백: URL을 일부러 깨뜨린 상태에서
   폴백 배경이 즉시 뜨고 "그리는 중" 칩이 안 뜨는지 스크린샷(_shots/).
3. 서버·브라우저 종료, 테스트 방 saves 삭제, 잔류 프로세스 체크.
4. 구현 전 mcp__codex-bridge__ask_codex 계획 검토, 구현 후 검증.
5. 같은 문제 2회 실패 시 중단하고 상황 보고.
