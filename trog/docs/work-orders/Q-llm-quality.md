# [패키지 Q] DM 텍스트 품질 게이트 — 한국어 붕괴·영단어 혼입 방어

사용자 제보(2026-07-15): "킹짱룡이 … 치솟는다고 주변을 살폈다. 그 유령은 달러가 아니라, 내 목을
조여온 accelerator를 일으킨 유령이다!" — 비문+영단어 혼입, 점점 악화.

Fable 진단 확정:
- **근본 원인은 모델 품질**: LLM_MODE=wrapper의 주 모델이 무료 중계(gemini-3-flash-preview,
  폴백 qwen3.5)라 한국어가 약함. 진짜 해법은 실전 세션을 좋은 모델로(.env — 사용자 결정 사항,
  이 패키지 범위 아님).
- **악화 메커니즘**: 깨진 응답이 히스토리에 들어가 다음 응답이 모방(오염 루프).
- 노란/흰 글씨 번갈이는 renderer의 따옴표 하이라이트(formatDmInline 3608)가 깨진 텍스트의
  홀짝 따옴표에 정직하게 반응한 **증상**이지 renderer 버그가 아님 — 상류를 고친다.

작업 폴더: `C:\Users\ui2030\Documents\trpg\trog`. 공통 규칙: 코드 먼저 읽기·미커밋 위에 얹기·
스타일 준수·커밋 금지·IP 고유명사 금지. **.env는 절대 수정 금지**(사용자 결정 영역).

## Q-1. 응답 품질 게이트 + 1회 재시도 (main.py)

1. `_dm_text_quality_ok(text) -> Tuple[bool, str]` 순수 함수:
   - 태그([...])·마크다운·주사위 뱃지·숫자 제거 후 본문만 평가.
   - 판정(레버는 모듈 상수): 라틴 알파벳 4자 이상 연속 "단어"가 5개 초과 → fail("latin"),
     한글 비율(한글/(한글+라틴)) < 0.75 → fail("hangul_ratio"). 본문 80자 미만이면 무조건 pass(짧은
     응답 오탐 방지).
   - 고유명사 예외는 만들지 마라(과설계) — 실패해도 아래처럼 1회 재시도 후 수용이라 치명적이지 않음.
2. DM 서사 생성 경로(player_action의 llm_complete 호출부, monster_turn 경로 포함 — grep으로 전부)에서:
   품질 fail 시 **1회만** 재시도. 재시도는 같은 llm_complete 호출에 시스템 프롬프트 말미
   "직전 응답이 손상되어 재생성한다. 반드시 자연스러운 한국어로만 서술하라." 1줄 덧붙여.
   재시도도 fail이면 그 텍스트 그대로 사용(게임 멈춤 금지) + logger.warning("[DM_QUALITY] ...", 사유).
3. **오염 루프 차단**: 최종적으로 품질 fail인 응답은 room.messages 히스토리에 넣을 때
   `[품질 저하 응답 — 요약 불가]` 로 대체하지 말고 **그대로 넣되**, 다음 요청의 히스토리 조립 시
   fail 마킹된 항목을 건너뛰게 하라(메시지 dict에 `quality_bad: True` 플래그, 히스토리 선별부에서 제외).
   플레이어 화면에는 그대로 보여준다(뭐가 나왔는지 사용자는 봐야 함).
4. 시스템 프롬프트(build_system_prompt 1630)에 1줄 추가: "본문은 자연스러운 한국어로만 쓴다.
   영어 단어·로마자를 섞지 않는다(고유 지명·인명도 한글 표기)."

## Q-2. 테스트

_test_dm_quality.py: 정상 한국어 pass / 영단어 5개+ fail / 한글비율 낮음 fail / 80자 미만 pass /
태그·주사위 뱃지 포함 텍스트에서 태그 제외 평가 / quality_bad 히스토리 제외.

## 검증 (전부)
1. py_compile. 신규 테스트 + 회귀(_test_balance, _test_act_progress) 통과.
2. 실동(GAME_PORT=8099): 정상 플레이 1턴 — 게이트가 정상 응답을 막지 않는지(로그에 QUALITY 경고
   없음) 확인 → 종료·정리.
3. 구현 전 mcp__codex-bridge__ask_codex 계획 검토, 구현 후 검증.
4. 같은 문제 2회 실패 시 중단하고 상황 보고.
