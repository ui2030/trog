# TROG — runtime dependencies
# Pinned to tested-good minimum versions with permissive upper bounds.
# Bump minima when you verify a newer release; keep upper bounds to avoid surprise breakage.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
anthropic>=0.40,<1.0
openai>=1.50,<2.0
python-dotenv>=1.0,<2.0
